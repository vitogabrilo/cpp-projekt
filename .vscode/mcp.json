{
  "servers": {
    "appmap": {
      "type": "stdio",
      "command": "${userHome}/.appmap/bin/appmap",
      "args": [
        "query",
        "mcp"
      ]
    },
    "appmap-gold-traces": {
      "type": "stdio",
      "command": "${userHome}/.appmap/bin/appmap",
      "args": [
        "query",
        "mcp",
        "--appmap-dir",
        "gold_traces/baseline/appmaps",
        "--query-db",
        "tmp/gold_traces_query.db"
      ]
    }
  }
}
